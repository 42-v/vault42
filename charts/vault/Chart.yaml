apiVersion: v2
name: vault-auth
description: Production-grade JWT authentication microservice
type: application
version: 1.0.0
appVersion: "1.0.0"
keywords:
  - authentication
  - jwt
  - security
  - oauth2
  - totp
home: https://github.com/42-v/vault42
# The organisation's avatar. The repository carries no image asset of its own, so
# this is the one icon URL that exists and resolves rather than a path invented to
# quiet the linter. Verified 200 image/png.
icon: https://github.com/42-v.png
sources:
  - https://github.com/42-v/vault42
maintainers:
  - name: V
    email: vault@42-v.com
