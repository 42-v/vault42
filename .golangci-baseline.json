{
  "_comment": [
    "golangci-lint findings per linter across ./..., as of the 1.0.0 hardening",
    "pass. The full run is clean, so there is nothing left to ratchet: every",
    "linter's allowance is zero and the CI step reads a missing key as zero,",
    "which makes any finding at all a regression that fails the build.",
    "It is not a suppression list and it disables nothing. New code is also",
    "gated separately by --new-from-merge-base.",
    "This file can be deleted once the CI step it feeds is rewritten as a plain",
    "`golangci-lint run ./...`; while it exists it must stay empty."
  ],
  "findings_by_linter": {},
  "total": 0
}
