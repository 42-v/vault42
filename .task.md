# Task 005

Build a standalone static marketing/product landing page for vault42 (served at vault.42-v.com). Concept codename: ROOT OF TRUST (security-corpo, credibility-first).

SCOPE / PLACEMENT (HARD CONSTRAINTS):
- Create a NEW self-contained directory `site/` at the repo root: index.html, styles.css, app.js, plus any local assets. No build step (plain HTML/CSS/vanilla JS).
- DO NOT modify or touch: web/ (the existing Vue/Vite app), internal/, cmd/, admin-gateway/, charts/, k8s/, vault/, or ANY Go/Vue/TS source. ADD ONLY under site/. Purely additive.

DESIGN SYSTEM (canonical source mounted read-only at the path /allow/branding):
- Read /allow/branding/DESIGN-LANGUAGE.md and /allow/branding/components/console.css and derive the tokens VERBATIM.
- AMOLED #000000 void only; #00FF42 phosphor as the single primary trace; secondary neon used SPARINGLY: cyan #00E5FF = data/verified, magenta #FF2E92 = threat/attacker, danger #ff3b3b = hard reject only. DROP violet/amber on this page (austere register).
- Monospace everywhere (Berkeley Mono / JetBrains Mono stack), uppercase tracked labels, glass bloom over the void, modern high-end CRT atmosphere (subtle scanlines + film grain + vignette + faint chromatic aberration) — NOT retro-kitsch; fast + ultra legible.
- #8EE517 (Evey's hair) must NEVER appear anywhere in the UI.
- Full prefers-reduced-motion guards (disable rain/scan/flicker/loops; keep focus rings + essential state). WCAG AAA contrast. >=44px tap targets. Fully keyboard accessible.

HERO — the LIVE TRUST CHAIN (centerpiece, animated, vanilla JS):
- vault42 (ISSUER, RS256) issues a decoded JWT showing header / payload / signature.
- Two services verify it via JWKS -> cyan "VERIFIED".
- One ATTACKER fires alg:none / HS256-confusion / forged-jku -> magenta "REJECTED", demonstrating the RS256-only algorithm-confusion defense.
- Loop the demonstration; on reduced-motion show the final resolved state (no animation).
- Headline: "OWN THE ROOT OF TRUST." Subhead: self-hosted identity authority, one binary. CTAs: [DEPLOY] (primary) + [BOOK A BRIEFING] (ghost). Trust strip: MIT . Go . RS256 . JWKS . runs on a Pi.

SECTIONS (in order):
1. Hero + live trust chain.
2. What it is — self-hosted identity authority; one Go binary; issues all session JWTs; RS256/JWKS resource-server model. READ the repo README.md, SECURITY.md, docs/ for ACCURATE facts — do not invent.
3. Proof points — the REAL differentiators from the repo (e.g. KMS/unwrap oracle, Shamir, FIDO2/Nitrokey, post-quantum where true, append-only audit). Cite only what the repo actually supports.
4. AUDIT — a small append-only, hash-chained ledger strip mirroring the DB-enforced append-only audit log: entries chained by hash, sealed with a root hash. Distinctive, not gimmicky.
5. Self-host quickstart — copy-paste deploy (helm/binary) with a copy button.
6. Footer CTAs — DEPLOY / READ SOURCE (github) / DOCS; version v0.8.0 . MIT.

COPY: terminal-precise, security-corpo, "show don't tell". Credible to a sharp security buyer.

QUALITY: production-grade, Awwwards/FWA-calibre but FAST + ultra legible. Self-contained. Add site/README.md noting how to preview (open index.html) and where it should be wired for serving at vault.42-v.com. Do not push or open a PR — leave the work on the task branch for review.
